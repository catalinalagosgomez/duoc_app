export interface Inscripcion {
    uidAlumno: string;         // El UID del alumno que se inscribe
    idAsignatura: string;      // El ID de la asignatura
    nombreAsignatura: string;  // El nombre de la asignatura // La fecha en la que se realizó la inscripción
  }
  